<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newsburger="{{devoured}}">
		{{#if devoured}}DEVOURED{{else}}EAT{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>