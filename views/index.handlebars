<h1 style="text-align: center;">Burgers!</h1>

<h2 style="text-align: left;">Burgers that are not devoured!</h2>

<ul style="text-align: left;">
  {{#each burgers}}
  {{#unless devoured}}
  {{> burgers/cat-block sleep=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2 style="text-align: right;">burgers that are devoured!</h2>

<ul style="text-align: right;">
  {{#each burgers}}
  {{#if devoured}}
  {{> burgers/cat-block sleep=false}}
  {{/if}}
  {{/each}}
</ul>

<div class="container;" style="text-align: center;"></div>
<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> devoured!<br>
    <input type="radio" name="devoured" value="0"> Awake
  </div>

  <button class="btn" type="submit">
    <p class="btnimg">ADD NEW</p>
    <img style="height: 2.5rem; width: 3rem;" src="public/assets/image/icon.png" />
  </button>
</form>
</div>